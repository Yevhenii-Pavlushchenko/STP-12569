import './js/burgerMenu';
import './js/how-to-play-swiper';
import './js/features-swiper';
import './js/gallery-swiper';

// Проверка на элементы, которые могут вызывать горизонтальную прокрутку
//document.querySelectorAll('*').forEach(el => {
//   if (el.offsetWidth > document.documentElement.offsetWidth) {
//     console.log('Вылетает вот этот парень:', el);
//   }
// });
